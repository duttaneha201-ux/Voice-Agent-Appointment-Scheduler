GROQ_API_KEY=your-groq-api-key

# Phase 2: Google — use either set of names
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=path/to/service-account.json
# Or: GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
GOOGLE_CALENDAR_ID=primary
GOOGLE_SHEET_ID=
# Advisor / draft recipient (Gmail draft is sent to this address)
GMAIL_USER=advisor@example.com
# Or: ADVISOR_EMAIL=advisor@example.com
# GMAIL_APP_PASSWORD=  (optional; not used by current Gmail API service-account flow)

# Optional: base URL for secure links (default https://example.com)
# BASE_URL=https://example.com
TIMEZONE=Asia/Kolkata

